# Array Methods

* Some Array attributes and methods to be familiar with
    * `length`
    * `pop()`
    * `push()`
    * `shift()`
    * `unshift()`
    * `slice()`
    * `splice()`
    * `split()`
    * `forEach()`, `map()`, `filter()`
